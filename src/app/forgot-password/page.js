import Placeholder from "@/components/Placeholder";

// Auth route — rendered without the app shell by AppChrome.
export default function ForgotPasswordPage() {
  return (
    <Placeholder standalone title="Forgot Password" note="Coming in a later milestone." />
  );
}
