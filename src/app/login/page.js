import Placeholder from "@/components/Placeholder";

// Auth route — rendered without the app shell by AppChrome.
export default function LoginPage() {
  return <Placeholder standalone title="Login" note="Coming in a later milestone." />;
}
