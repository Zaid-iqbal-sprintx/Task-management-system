import Placeholder from "@/components/Placeholder";

// Rendered inside the app shell (navbar + main) via AppChrome.
export default function TasksPage() {
  return <Placeholder title="Tasks" note="Coming in a later milestone." />;
}
