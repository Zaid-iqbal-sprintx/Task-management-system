// Temporary placeholder so we can verify the layout + navigation shell.
// This gets replaced with the real task list (search, filters, mock data)
// in the next step.
export default function TasksPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-slate-900">Tasks</h1>
      <p className="mt-2 text-slate-600">
        Layout and navigation shell is working. The task list will go here next.
      </p>
    </div>
  );
}
