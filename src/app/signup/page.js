import Placeholder from "@/components/Placeholder";

// Auth route — rendered without the app shell by AppChrome.
export default function SignUpPage() {
  return <Placeholder standalone title="Sign Up" note="Coming in a later milestone." />;
}
