# Copy this file to ".env" and adjust if your backend runs elsewhere.
# .env is gitignored — only this example is committed.

# Base URL of the task backend (CRUD_BACKEND). Exposed to the browser, so it
# must be prefixed with NEXT_PUBLIC_.
NEXT_PUBLIC_BACKEND_URL=http://localhost:5002
